const Comentar = (conteudoCampoInput, usuario) => {

    return novoComentario = {
        date: Date.Now,
        text: conteudoCampoInput,
        userName: usuario
    }
    
}

export default Comentar;